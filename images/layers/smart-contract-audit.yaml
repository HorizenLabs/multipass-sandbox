#cloud-config
# MPS Layer: smart-contract-audit
# Solidity security tools (on top of smart-contract-dev)

x-mps:
  description: "smart-contract-dev + Slither + Mythril (amd64) + Echidna + Medusa + Halmos (amd64)"
  disk_size: 14G
  min_profile: standard
  min_disk: 15G
  min_memory: 4G
  min_cpus: 4

final_message: "MPS smart-contract-audit image provisioning complete."
